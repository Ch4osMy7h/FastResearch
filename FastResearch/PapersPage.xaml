<Page
    x:Class="FastResearch.PapersPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FastResearch"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Page.Background>
        <ImageBrush Stretch="UniformToFill" ImageSource="/Assets/timg.png"/>
    </Page.Background>

    <GridView ItemsSource="{x:Bind ViewModel.PaperAreas}" 
              HorizontalAlignment="Left">
        <GridView.ItemTemplate>
            <DataTemplate x:DataType="local:PaperArea">
                <StackPanel Orientation="Vertical" Margin="4">
                    <SymbolIcon Symbol="Document"/>
                    <RelativePanel>
                        <TextBlock Text="{x:Bind Name}" Name ="PaperName" FontWeight="Bold"/>
                        <TextBlock Text="{x:Bind Description}" RelativePanel.AlignTopWith="PaperName"/>
                    </RelativePanel>
                </StackPanel>
            </DataTemplate>
        </GridView.ItemTemplate>
    </GridView>
</Page>
