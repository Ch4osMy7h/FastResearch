<ContentDialog
    x:Class="FastResearch.DetailDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FastResearch"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="编辑"
    PrimaryButtonText="导出"
    SecondaryButtonText="保存"
    CloseButtonText="取消"
    PrimaryButtonClick="ContentDialog_PrimaryButtonClick"
    SecondaryButtonClick="ContentDialog_SecondaryButtonClick"
    CloseButtonClick="ContentDialog_CloseButtonClick">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60" />
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <TextBox Header="Name:" x:Name="nameTextBox" Text="{x:Bind command.name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Grid.Row="0"/>
        <TextBox Header="Description" x:Name="descriptionTextBox" Text="{x:Bind command.description, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Grid.Row="1"/>
        <TextBlock x:Name="messageTextBlock" Grid.Row="2"/>
    </Grid>
    
</ContentDialog>
