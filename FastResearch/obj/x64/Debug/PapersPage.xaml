<Page x:ConnectionId='1'
    x:Class="FastResearch.PapersPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FastResearch.Model"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converter="using:FastResearch.Converter"
    mc:Ignorable="d">
    <Page.Background>
        <ImageBrush Stretch="UniformToFill" ImageSource="/Assets/timg.png"/>
    </Page.Background>

    <Page.Resources>
        <DataTemplate x:Key="NavLinkItemTemplate"                             >
            <StackPanel x:ConnectionId='4' Orientation="Horizontal" Margin="0,0,0,0" AutomationProperties.Name="PaperArea">
                <TextBlock x:ConnectionId='5'                       Margin="0,0,0,0" VerticalAlignment="Center" Height="Auto" FontWeight="Bold"/>
            </StackPanel>
        </DataTemplate>
        <ControlTemplate x:Key="SplitViewTogglePaneButtonTemplate"
                         TargetType="ToggleButton">
            <Grid x:Name="RootGrid"
                  Background="{TemplateBinding Background}">
                <VisualStateManager.VisualStateGroups>
                    <VisualStateGroup x:Name="CommonStates">
                        <VisualState x:Name="Normal" />
                        <VisualState x:Name="PointerOver">
                            <VisualState.Setters>
                                <Setter Target="RootGrid.Background" Value="{ThemeResource SystemControlBackgroundBaseLowBrush}" />
                                <Setter Target="ContentPresenter.Foreground" Value="{ThemeResource SystemControlHighlightBaseMediumHighBrush}" />
                            </VisualState.Setters>
                        </VisualState>
                        <VisualState x:Name="Pressed">
                            <VisualState.Setters>
                                <Setter Target="RootGrid.Background" Value="{ThemeResource SystemControlBackgroundBaseMediumLowBrush}" />
                                <Setter Target="ContentPresenter.Foreground" Value="{ThemeResource SystemControlHighlightBaseMediumBrush}" />
                            </VisualState.Setters>
                        </VisualState>
                        <VisualState x:Name="Disabled">
                            <VisualState.Setters>
                                <Setter Target="ContentPresenter.Foreground" Value="{ThemeResource SystemControlForegroundBaseLowBrush}" />
                            </VisualState.Setters>
                        </VisualState>
                        <VisualState x:Name="Checked"  />
                        <VisualState x:Name="CheckedPointerOver">
                            <VisualState.Setters>
                                <Setter Target="RootGrid.Background" Value="{ThemeResource SystemControlBackgroundBaseLowBrush}" />
                                <Setter Target="ContentPresenter.Foreground" Value="{ThemeResource SystemControlHighlightBaseMediumHighBrush}" />
                            </VisualState.Setters>
                        </VisualState>
                        <VisualState x:Name="CheckedPressed">
                            <VisualState.Setters>
                                <Setter Target="RootGrid.Background" Value="{ThemeResource SystemControlBackgroundBaseMediumLowBrush}" />
                                <Setter Target="ContentPresenter.Foreground" Value="{ThemeResource SystemControlHighlightBaseMediumBrush}" />
                            </VisualState.Setters>
                        </VisualState>
                    </VisualStateGroup>
                </VisualStateManager.VisualStateGroups>


                <ContentPresenter x:Name="ContentPresenter"
                                  Padding="{TemplateBinding Padding}"
                                  BorderBrush="{TemplateBinding BorderBrush}"
                                  BorderThickness="{TemplateBinding BorderThickness}"
                                  HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                  VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"
                                  AutomationProperties.AccessibilityView="Raw"
                                  ContentTemplate="{TemplateBinding ContentTemplate}"
                                  ContentTransitions="{TemplateBinding ContentTransitions}" />
            </Grid>
        </ControlTemplate>

        <Style x:Key="SplitViewTogglePaneButtonStyle"
               TargetType="ToggleButton">
            <Setter Property="Background"
                    Value="Transparent" />
            <Setter Property="Foreground"
                    Value="{ThemeResource SystemControlForegroundBaseHighBrush}" />
            <Setter Property="BorderBrush"
                    Value="{ThemeResource SystemControlForegroundBaseHighBrush}" />
            <Setter Property="BorderThickness"
                    Value="0" />
            <Setter Property="Padding"
                    Value="0" />
            <Setter Property="HorizontalAlignment"
                    Value="Left" />
            <Setter Property="HorizontalContentAlignment"
                    Value="Center" />
            <Setter Property="VerticalAlignment"
                    Value="Top" />
            <Setter Property="VerticalContentAlignment"
                    Value="Center" />
            <Setter Property="UseSystemFocusVisuals"
                    Value="True" />

            <Setter Property="FontFamily"
                    Value="{ThemeResource SymbolThemeFontFamily}" />
            <Setter Property="Content"
                    Value="&#xE700;" />
            <Setter Property="Height"
                    Value="48" />
            <Setter Property="Width"
                    Value="48" />

            <Setter Property="FontWeight"
                    Value="Normal" />
            <Setter Property="FontSize"
                    Value="20" />
            <Setter Property="Template"
                    Value="{StaticResource SplitViewTogglePaneButtonTemplate}" />
        </Style>
    </Page.Resources>

    <Grid>
        <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE700;"/>
        <SplitView x:ConnectionId='6' x:Name="splitView" PaneBackground="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}"
               IsPaneOpen="True" DisplayMode="Inline">
            <SplitView.Pane>
                <Grid Margin="0,0,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="48"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="48"/>
                    </Grid.RowDefinitions>
                    <Button x:ConnectionId='7' Content="PaperArea" Name="AreaButton" Grid.Row="0"                          Width="321" Height="48" />
                    <!--<ComboBox ItemsSource="{x:Bind ViewModel.PaperAreas}" 
                                             Width="321" Grid.Row="0" IsEditable="True"/>-->
                    <ListView x:ConnectionId='8'  x:Name="NavLinksList" Margin="0,0,0,0" SelectionMode="None" Grid.Row="1" VerticalAlignment="Stretch"
                                                               IsItemClickEnabled="True"
                                                                       ItemTemplate="{StaticResource NavLinkItemTemplate}"
                          
                          />
                    <Button x:ConnectionId='9' Grid.Row="2" 
                            Content="Add Paper" 
                            Height="48"
                            HorizontalAlignment="Center" 
                            VerticalAlignment="Center"                           
                            x:Name="NewAreaButton" 
                            Background= "{ThemeResource SystemControlBackgroundChromeMediumLowBrush}"
                            BorderBrush="LightGray"  
                           Width="321"
                            >
                        <Button.Flyout>
                            <Flyout>
                                <StackPanel>
                                    <TextBox x:ConnectionId='10' Name ="PaperItemInputBox" PlaceholderText="Input" Margin="0,0,0,12" />
                                    <Button x:ConnectionId='11' Name="PaperItemBindButton" Content="Bind"                                  />
                                    <Button x:ConnectionId='12' Name="PaperItemSaveButton" Content="Save"                                  />
                                </StackPanel>
                            </Flyout>
                        </Button.Flyout>

                    </Button>
                </Grid>
            </SplitView.Pane>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <CommandBar Background="Transparent" IsOpen="False" DefaultLabelPosition="Right" Grid.Row="0" HorizontalAlignment="Left">
  
                    <AppBarButton Icon="OpenFile" Label="Open"/>
                    <!--<AppBarButton Icon="Bold" Label="Bind" Click="AppBarButtonBind_Click"/>-->
                    <AppBarButton Icon="Delete" Label="Delete"/>
                    <AppBarButton Icon="SolidStar" Label="Mark"/>
                    <CommandBar.SecondaryCommands>
                        <AppBarButton Icon="Setting" Label="Settings"/>
                    </CommandBar.SecondaryCommands>
                </CommandBar>
                <!--<FlipView Grid.Row="1" Name="PdfView">
                    <FlipView.ItemTemplate>
                        <DataTemplate>
                            <ScrollViewer ZoomMode="Enabled" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                                <Image Source="{Binding}"/>
                            </ScrollViewer>
                        </DataTemplate>
                    </FlipView.ItemTemplate>
                </FlipView>-->
            </Grid>
        </SplitView>
        
    </Grid>

</Page>

